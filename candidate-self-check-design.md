# Design: AI-Native 面试者自查向导

Generated by /office-hours on 2026-03-23
Branch: main
Repo: toolAssistant/ai-native-interview-check
Status: APPROVED
Mode: Builder

## Problem Statement

当前产品是一份给面试官使用的 AI-Native 工程师招聘手册：长文档结构，包含岗位定义、题库、评分标准、否决项和评分记录表。

目标不是简单把文案改成候选人视角，而是把它重构成一个给候选人自己使用的交互式 HTML 自查工具。用户需要从候选者角度逐题作答，在每一步完成后查看评分标准、给自己打分，并在最后看到累计结果、岗位倾向和能力短板。

一句话定义：

> 把“面试官用来判断你值不值得录用的手册”，改造成“候选人用来发现自己短板并补齐的训练向导”。

## What Makes This Cool

- 不是读手册，而是边答边练，交互上更像产品而不是文章。
- 用户先暴露自己的真实回答，再看 rubric，避免一开始就被“标准答案”污染。
- 结果页不只给总分，而是把分数翻译成可行动的反馈，比如“你会做，但不会把要求说清楚”。
- 原始手册已经有很强的结构化内容，改造后第一版就能很扎实，不需要凭空创造题库。

## Constraints

- 第一版继续保持单文件 HTML、零依赖、浏览器直接打开可用。
- 现有题库、评分标准、Builder/Reviewer 能力模型尽量复用。
- 第一版不引入账号体系、后端存储或复杂协作功能。
- 用户目标是自查和训练，不是生成正式的招聘结论。
- 反馈语气必须从“裁判”切换成“教练”，但不能失去锋利度。

## Premises

1. 这个产品的核心价值是帮助候选人发现短板并补齐，而不是模拟被打分的压迫感。
2. 结果页不能只给总分，必须解释用户弱在哪类能力。
3. 现有题库可以大量复用，但页面结构必须从“内容展示”改成“任务流程”。

## Approaches Considered

### Approach A: 最小改版自查流

保留现有长文档结构，把每道题改成候选人可填写的题目，增加“我的回答”和“我的自评分”，最后自动累计总分。

优点：
- 改动最小，上线最快
- 内容复用率最高
- 对当前单文件 HTML 结构最友好

问题：
- 本质仍然像“会填写的手册”
- 用户完成感和训练感不足
- 结果页很容易只剩下总分，没有足够的诊断解释

### Approach B: 数据驱动诊断向导

把整份内容改造成 step-by-step 自查流。用户先选“未确定 / Builder / Reviewer”路线，再逐题作答；每题完成后解锁评分标准、自评打分和即时反馈；最后生成总分、能力诊断、岗位倾向和补强建议。

优点：
- 最贴合“先回答，再看标准，再打分，最后累计”的使用方式
- 用户体验明显更像产品，而不是文档
- 后续可自然扩展为“只练通用题”“只练 Builder”“重做错题”

问题：
- 需要重构当前页面信息架构
- 需要把题目、rubric、权重整理成数据模型
- 结果解释需要谨慎设计，避免显得武断

### Approach C: 镜像面试训练器

把页面做成“面试官发问 / 候选人作答 / 系统揭示低分信号”的模拟面试体验，强化真实面试代入感。

优点：
- 产品辨识度高
- 更容易让用户理解自己为什么会被卡
- 适合传播和展示

问题：
- 第一版范围偏大
- 容易把注意力放到“氛围”而不是“诊断”
- 标准化结果页不如 Approach B 稳

## Recommended Approach

选择 **Approach B: 数据驱动诊断向导**。

原因：

- 用户已经明确想要“逐步回答问题，每一步完成后看评分标准，给自己打分，最后累计分数”，这天然对应向导式交互，而不是长文档。
- Approach B 能把原手册最有价值的内容资产保留下来，同时把信息组织方式从“展示知识”变成“驱动动作”。
- 它比 Approach A 更完整，但仍然能保持单文件 HTML 的实现边界，不会把第一版做成大工程。

### Recommended User Flow

1. 进入首页，先理解这不是考试结果，而是一套自查训练。
2. 用户选择路线：
   - 未确定：先做 3 个通用模块，再根据表现推荐 Builder 或 Reviewer
   - Builder：通用模块 + Builder 强化
   - Reviewer：通用模块 + Reviewer 强化
3. 每道题固定经历四步：
   - 先独立作答
   - 再展开评分标准
   - 然后自评打分
   - 最后收到一句即时反馈
4. 页面持续显示：
   - 当前进度
   - 当前累计分数
   - 当前能力判断
5. 结果页输出：
   - 总分与模块分
   - Builder / Reviewer 倾向
   - 2-3 条强项总结
   - 2-3 条短板诊断
   - 下一步训练建议

### Recommended Information Architecture

- `首页 / 起始页`
  - 产品定位
  - 使用说明
  - 路线选择

- `答题页`
  - 题目区
  - 作答区
  - rubric 解锁区
  - 自评分区
  - 即时反馈区

- `进度侧栏`
  - 当前模块
  - 已完成题数
  - 当前累计得分
  - 当前倾向判断

- `结果页`
  - 综合结论
  - 维度拆分
  - 风险信号
  - 下一步建议

### Content Mapping from Current Handbook

- “两种岗位定义” → 结果页的人格倾向解释，而不是首页就要求用户自我归类
- “通用题库 / Builder / Reviewer 题库” → 诊断向导的问题池
- “评分标准” → 每题解锁后的 rubric
- “综合评分权重” → 最终结果页的分项权重说明
- “一票否决项” → 改成“高风险信号”，用于提醒用户而不是直接贴失败标签
- “评分记录表” → 改成“个人诊断面板”，不再模拟面试官提交动作

### Tone Shift

原页面语言：
- 偏判断、偏淘汰、偏招聘场景

新页面语言：
- 偏诊断、偏训练、偏补齐短板

例子：
- “不能接受 AI 比自己写得好” → “如果你对 AI 产出有强防御心态，这会成为协作瓶颈”
- “只关注技术实现，从不问为什么要做这个” → “如果你总是直接进入实现，说明系统思维还不够稳定”

### First-Version Guardrails

- 不做账号登录
- 不做正式导出报告
- 不做自动评分 AI 评判
- 不做多人对比
- 不做复杂动画或游戏化

第一版只验证一件事：

> 候选人是否愿意按这个流程认真作答，并且在结束后能清楚知道自己弱在哪。

## Open Questions

1. 第一版是否允许用户直接查看所有题目，还是必须按流程解锁？
2. 结果页是否要显示明确等级（如 PASS / HOLD / FAIL），还是只显示诊断型描述？
3. “高风险信号”是否保留原手册里的 6 条，还是缩成更适合自查的 3-4 条？
4. 未确定路线的用户，是做完通用题后自动推荐路线，还是允许自由切换？

## Success Criteria

- 用户在 1 分钟内理解这不是阅读页，而是自查流程。
- 用户能在不看标准答案的情况下完成至少 1 道题。
- 用户完成单题后，能理解 rubric 和自评分之间的关系。
- 用户结束后能明确说出至少 2 个自己的短板，而不只是记住一个总分。
- 页面仍然保持单文件 HTML、浏览器直接打开可用。

## Next Steps

1. 把现有 14 道题抽成统一的数据结构：模块、题干、等级、rubric、所属路线。
2. 先做“未确定路线”的完整闭环，验证单题流程是否成立。
3. 设计结果页文案：总分、倾向、短板、建议四块必须同时成立。
4. 再把 Builder / Reviewer 强化路径接回去。
5. 最后再处理视觉精修，而不是一开始就做美化。

## What I noticed about how you think

- 你不是说“帮我把它改得更像候选人页面”，你说的是“让用户从候选者的角度，一一回答问题”。
- 你第一反应不是“换文案”，而是“每一步完成后，看评分标准，给自己打分”。
- 你在方案选择上从 `A` 切到 `B`，说明你最后更在意产品使用方式，而不是最小改动。
